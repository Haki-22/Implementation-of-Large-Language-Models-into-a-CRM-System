"""UC-03 evaluation: the synthetic Czech dictation corpus and the Whisper runs.

``generate_synth_audio`` builds the corpus, ``run_synth_eval`` scores a Whisper
size on it, ``metrics`` holds the pure scoring functions. ``README.md`` says what
each reported number is and is not; the evaluation of the middleware itself has
not run yet.
"""
